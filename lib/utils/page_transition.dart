import 'package:get/route_manager.dart';

class AppPageTransition {
  AppPageTransition._();
  static const Transition zoomPageTransition = Transition.zoom;
  static const Transition leftToRightTransition = Transition.leftToRight;
}
